# Staged copies of compiled artifacts — populated by `pixi run -e wasm
# serve_tinyc_demo`. The originals live at repo root after
# `pixi run -e wasm build_tinyc_wasm`.
tinyc.wasm
corec_runtime.wasm.o
_dist/
