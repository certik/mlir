// `Td *p;` declares a pointer-to-typedef variable; the parser must
// recognize this even though `*` (not an IDENT) immediately follows the
// typedef name.
typedef struct point_s { int x; int y; } Point;

int main() {
    Point a;
    a.x = 3;
    a.y = 4;

    Point *p;
    p = &a;
    print(p->x);     // 3
    print(p->y);     // 4

    Point *q = &a;
    print(q->x + q->y);  // 7
    return 0;
}
