extern int printf(const char *fmt, ...);

static char* g_p = ((void*)0);
static const int g_shifted = 1 << 8;

int main() {
    int is_null = (g_p == 0);
    printf("%d %d\n", is_null, g_shifted);
    return 0;
}

