extern int printf(const char *fmt, ...);
struct R { int a; int b; };

int main() {
    struct R one;
    one.a = 100; one.b = 200;
    struct R *p = &one;
    printf("%lld\n", (long long)((int64_t)p[0].a));
    printf("%lld\n", (long long)((int64_t)p[0].b));
    return 0;
}
