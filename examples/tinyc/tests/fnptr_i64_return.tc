extern int printf(const char *fmt, ...);
typedef long (*Fn)(int);

long mul100(int x) {
    return (long)x * 100;
}

int main() {
    Fn f = mul100;
    long r = f(7);
    printf("%lld\n", (long long)((int)r));
    long r2 = f(20);
    printf("%lld\n", (long long)((int)r2));
    return 0;
}
