extern int printf(const char *fmt, ...);
int main() {
    int x = 10;
    int* p = &x;
    *p = 42;
    printf("%lld\n", (long long)(x));    // 42
    printf("%lld\n", (long long)(*p));   // 42
    return 0;
}
