struct Pair { int a; int b; };

struct Pair make(int a, int b) {
    struct Pair p; p.a = a; p.b = b;
    return p;
}

int main() {
    _tinyc_print(make(7, 11).a);
    _tinyc_print(make(7, 11).b);
    _tinyc_print(make(100, 200).a + make(3, 4).b);
    return 0;
}
