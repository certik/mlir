struct S { int a; int b; };

struct S make(int x) {
    struct S s;
    s.a = x;
    s.b = x * 10;
    return s;
}

typedef struct S (*Fn)(int);

int main() {
    Fn f = make;
    struct S r = f(7);
    _tinyc_print(r.a);
    _tinyc_print(r.b);
    _tinyc_print(r.a + r.b);
    return 0;
}
