extern int printf(const char *fmt, ...);
int sum_to(int n) {
    int i = 1;
    int s = 0;
    while (i <= n) {
        s = s + i;
        i = i + 1;
    }
    return s;
}

int main() {
    int x = sum_to(10);
    printf("%lld\n", (long long)(x));
    return 0;
}
